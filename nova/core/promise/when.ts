import Promise = require('./Promise');
import Deferred = require('./Deferred');
import promiseInterface = require('../interface/Promise');

/**
 * Shorthand for Promise.resolve
 * Usefull to keep ?
 **/
export = Promise.resolve;
